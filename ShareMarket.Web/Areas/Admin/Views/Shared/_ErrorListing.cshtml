@model List<string>


@if (Model.Count > 0)
{
    <div class="alert alert-error hide" style="display: block;margin:0 auto 10px; width:467px">
        <button data-dismiss="alert" class="close"></button>
        @string.Join("<br/>", Model.Select(x => x.ToString()))
    </div>
}
