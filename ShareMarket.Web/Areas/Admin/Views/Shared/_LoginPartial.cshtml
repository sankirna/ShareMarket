<ul class="nav pull-right">
    @if (Request.IsAuthenticated)
    {
        <text>

        <!-- BEGIN USER LOGIN DROPDOWN -->
        <li class="dropdown user">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <img src="" style="height: 29px;width: 29px;" />
                <span class="username">Hello, </span>
                <i class="icon-angle-down"></i>
            </a>
            <ul class="dropdown-menu">


                @using (Html.BeginForm("Logout", "Home", FormMethod.Post, new { id = "logoutForm" }))
                {
                    @Html.AntiForgeryToken()
                    <li class="myprofile"><a href="javascript:void(0);"><i class="icon-user"></i>My Profile</a></li>
                    <li><a href="javascript:document.getElementById('logoutForm').submit()"><i class="icon-key"></i>Log off</a></li>
                }

            </ul>
        </li>
        <!-- END USER LOGIN DROPDOWN -->

        </text>
    }
    else
    {
        <li>@Html.ActionLink("Log in", "Login", "Home", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    }
</ul>

